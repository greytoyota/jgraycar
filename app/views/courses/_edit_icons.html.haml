- if user_signed_in?
  %section.edit-icons
    = link_to image_tag("icons/edit.png"), edit_course_path(course)
    = link_to image_tag("icons/delete.png"), course_path(course), method: :delete,
      data: { confirm: "Are you sure you want to delete course #{course.title}?" }
