- if user_signed_in?
  %section.edit-icons
    = link_to image_tag("icons/edit.png"), edit_job_path(job)
    = link_to image_tag("icons/delete.png"), job_path(job), method: :delete,
      data: { confirm: "Are you sure you want to delete job #{job.title}?" }
